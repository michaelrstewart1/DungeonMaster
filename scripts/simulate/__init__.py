"""Multi-agent human-play simulation harness for DungeonMaster.

Drives the real backend (HTTP + WebSocket) with N bot players so end-to-end
gameplay can be tested iteratively without humans. See `plan.md` for design.
"""
__version__ = "0.1.0"
